const $=id=>document.getElementById(id);let docs=JSON.parse(localStorage.getItem('flipdocs')||'[]');let pending=null,current=null;
function save(){localStorage.setItem('flipdocs',JSON.stringify(docs))}function render(){let q=$('search').value.toLowerCase(),f=$('filter').value;let list=docs.filter(d=>(d.title.toLowerCase().includes(q))&&(f==='all'||(f==='locked'?d.password:f==='open'&&!d.password)));$('library').innerHTML=list.map(d=>`<article class="card"><div class="cover">▤</div><h3>${esc(d.title)}</h3><div class="meta">PDF · ${d.password?'🔒 ล็อกด้วยรหัสผ่าน':'เปิดอ่านได้'}</div><button class="primary" onclick="openDoc('${d.id}')">เปิดอ่าน</button></article>`).join('')||'<p class="muted">ยังไม่มีเอกสาร ลองอัปโหลด PDF แรกของคุณ</p>'}function esc(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
$('uploadBtn').onclick=()=>$('uploadDialog').showModal();$('uploadForm').onsubmit=e=>{e.preventDefault();pending={title:$('docTitle').value,password:$('docPassword').value};$('fileInput').click();$('uploadDialog').close()};$('fileInput').onchange=e=>{let file=e.target.files[0];if(!file)return;let r=new FileReader();r.onload=()=>{docs.unshift({id:crypto.randomUUID(),title:pending.title||file.name,password:pending.password,url:r.result,size:file.size});save();render();pending=null;$('fileInput').value=''};r.readAsDataURL(file)};
window.openDoc=id=>{current=docs.find(d=>d.id===id);$('readerTitle').textContent=current.title;$('readerGate').hidden=!current.password;$('pdfFrame').hidden=!!current.password;$('error').textContent='';$('readerPassword').value='';if(!current.password)$('pdfFrame').src=current.url;$('readerDialog').showModal()};$('unlockBtn').onclick=()=>{if($('readerPassword').value===current.password){$('readerGate').hidden=true;$('pdfFrame').hidden=false;$('pdfFrame').src=current.url}else $('error').textContent='รหัสผ่านไม่ถูกต้อง'};$('closeReader').onclick=()=>$('readerDialog').close();$('search').oninput=render;$('filter').onchange=render;render();
